# Security Automation Platform Configuration
# Generated automatically - customize as needed

# ==============================================================================
# LLM Provider Configuration
# ==============================================================================
# Options: ollama, gemini, openai
LLM_PROVIDER=ollama

# Ollama settings (default, included in docker-compose)
OLLAMA_MODEL=deepseek-coder:6.7b-instruct

# Optional: Gemini API (if you want to use Google's Gemini instead of Ollama)
# Get your key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=

# Optional: OpenAI API (if you want to use ChatGPT/GPT-4)
OPENAI_API_KEY=

# ==============================================================================
# Application to Scan
# ==============================================================================
# Path to YOUR application code that you want to scan
# Default: current directory (scans the platform itself as demo)
TARGET_APP_PATH=.

# ==============================================================================
# GitHub Integration (Optional - for automatic PR creation)
# ==============================================================================
# Get token from: https://github.com/settings/tokens
# Required scopes: repo, workflow
GITHUB_TOKEN=
GITHUB_REPO=

# ==============================================================================
# Notifications (Optional)
# ==============================================================================
# Slack webhook for notifications
SLACK_WEBHOOK_URL=

# Email notifications via SMTP
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
NOTIFICATION_EMAIL=
